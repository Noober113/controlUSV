float readDistance(int channel,int trig){
  // pinMode(SIG, OUTPUT);
  // pcf8574.digitalWrite(P0, muxTable[trig][0]);
  // pcf8574.digitalWrite(P1, muxTable[trig][1]);
  // pcf8574.digitalWrite(P2, muxTable[trig][2]);
  // pcf8574.digitalWrite(P3, muxTable[trig][3]);

  // digitalWrite(SIG,0);
  // delayMicroseconds(2);
  // digitalWrite(SIG,1);    
  // delayMicroseconds(10);
  // digitalWrite(SIG,0);    


  // pcf8574.digitalWrite(P0, muxTable[channel][0]);
  // pcf8574.digitalWrite(P1, muxTable[channel][1]);
  // pcf8574.digitalWrite(P2, muxTable[channel][2]);
  // pcf8574.digitalWrite(P3, muxTable[channel][3]);


  // pinMode(SIG, INPUT);
  // float duration = pulseIn(SIG, HIGH);
  // float distance=duration/2*0.0314;
  // return distance;    
}